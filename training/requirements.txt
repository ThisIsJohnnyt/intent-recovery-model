# torch/torchvision/torchaudio come from PyTorch's own CUDA-specific wheel
# index, not default PyPI -- cu128 matches the RTX 50-series (Blackwell)
# GPU this project trains on (see train.py's bf16 comment). On a machine
# with a different GPU/CUDA version, swap cu128 for the matching build
# from https://pytorch.org/get-started/locally/.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128
transformers==4.57.6
datasets==5.0.0
accelerate==1.14.0
sentencepiece==0.2.2
evaluate==0.4.6
optimum==2.1.0
optimum-onnx==0.1.0
onnx==1.22.0
onnxruntime==1.28.0
